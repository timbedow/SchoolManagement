@model SchoolManagement.Web.ViewModels.HomeViewModel

<h2>@Model.Title</h2>

@foreach (var student in Model.Students)
{
    <div>
        <h2>@student.FirstName @student.LastName</h2>
        <h3>@student.DateOfBirth.ToShortDateString()</h3>
    </div>
}
